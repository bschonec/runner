jobs:
  example_matrix:
    strategy:
      matrix:
        os: [ubuntu-22.04, ubuntu-20.04]
        version: [10, 12, 14]
    runs-on: ${{ matrix.os }}
    steps:

      - id: install-podman ${{ matrix.version }}
        run: echo ${{ matrix.version }}
        shell: bash

